function login(username: string): string {
    // console.log(username);

    const user = {
        name: "firozuddin",
    };
    const name = user.name;
    return username + name;
}

login("firoz");
